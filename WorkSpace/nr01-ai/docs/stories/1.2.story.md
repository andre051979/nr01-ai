---
id: "1.2"
title: "Autenticação e Login do Administrador"
epic: 1
story: 2
status: Draft
priority: high
type: security
created: 2026-02-25
updated: 2026-02-25
blockedBy: ["1.1"]
---

# Story 1.2: Autenticação e Login do Administrador

## Status: Draft

## Story Statement

Como administrador do sistema, quero fazer login com email e senha, para que eu possa acessar com segurança o painel de gestão de riscos psicossociais da empresa.

## Acceptance Criteria

- [ ] **AC1:** Página de login em `/login` com campos de email e senha, botão "Entrar" e validação de campos obrigatórios
- [ ] **AC2:** Autenticação via JWT — login bem-sucedido gera token armazenado em cookie httpOnly
- [ ] **AC3:** Rota `/` (dashboard) protegida — redireciona para `/login` se não autenticado
- [ ] **AC4:** Senha armazenada com bcrypt (salt rounds ≥ 12) — nunca em texto puro
- [ ] **AC5:** Mensagem de erro genérica em caso de credenciais inválidas ("Email ou senha incorretos") — sem vazamento de qual campo está errado
- [ ] **AC6:** Botão "Entrar" mostra estado de loading durante autenticação
- [ ] **AC7:** Após login bem-sucedido, redireciona para `/` (dashboard)
- [ ] **AC8:** Funcionalidade de logout disponível no layout do dashboard — limpa cookie e redireciona para `/login`
- [ ] **AC9:** Seed inicial cria usuário admin padrão: `admin@nr01.com` / `admin123` (alterável via env)

## Tasks / Subtasks

- [ ] **Task 1:** Criar API Route de autenticação (AC: 2, 4, 5)
  - [ ] Criar `packages/web/app/api/auth/login/route.ts` — POST
  - [ ] Validar body com Zod: `{ email: string, password: string }`
  - [ ] Buscar usuário no banco via Prisma por email
  - [ ] Comparar senha com `bcrypt.compare()`
  - [ ] Gerar JWT com `jose` library (payload: `{ userId, empresaId, role }`, exp: 8h)
  - [ ] Setar cookie `auth-token` httpOnly, secure, sameSite=lax
  - [ ] Retornar 401 com mensagem genérica em caso de erro

- [ ] **Task 2:** Criar API Route de logout (AC: 8)
  - [ ] Criar `packages/web/app/api/auth/logout/route.ts` — POST
  - [ ] Limpar cookie `auth-token`
  - [ ] Retornar redirect para `/login`

- [ ] **Task 3:** Criar middleware de proteção de rotas (AC: 3)
  - [ ] Criar `packages/web/middleware.ts`
  - [ ] Verificar cookie `auth-token` em rotas `/(dashboard)/*`
  - [ ] Redirecionar para `/login` se token inválido/ausente
  - [ ] Usar `jose` para verificação do JWT no edge runtime

- [ ] **Task 4:** Criar página de Login UI (AC: 1, 6, 7)
  - [ ] Criar `packages/web/app/(auth)/login/page.tsx`
  - [ ] Form com campos: email (type=email), senha (type=password)
  - [ ] Validação client-side com react-hook-form + Zod
  - [ ] Estado de loading no botão durante fetch
  - [ ] Exibir mensagem de erro da API
  - [ ] Layout centralizado, responsivo, com logo/nome do sistema

- [ ] **Task 5:** Criar botão de logout no layout (AC: 8)
  - [ ] Adicionar botão Logout em `packages/web/app/(dashboard)/layout.tsx`
  - [ ] Chamar POST `/api/auth/logout` e redirecionar

- [ ] **Task 6:** Seed do usuário admin (AC: 9)
  - [ ] Adicionar criação de usuário admin no `prisma/seed.ts`
  - [ ] Hash da senha com bcrypt antes de salvar
  - [ ] Usar env vars: `ADMIN_EMAIL`, `ADMIN_PASSWORD` com defaults

## Dev Notes

### Dependências a Instalar

```bash
npm install bcryptjs jose react-hook-form @hookform/resolvers zod
npm install -D @types/bcryptjs
```

### Estrutura do JWT

```typescript
// Payload do token
interface JwtPayload {
  userId: string;
  empresaId: string;
  role: string;
  iat: number;
  exp: number;
}
```

### Helper de Autenticação

Criar `packages/web/lib/auth.ts`:
```typescript
import { jwtVerify, SignJWT } from 'jose';
import { cookies } from 'next/headers';

const JWT_SECRET = new TextEncoder().encode(process.env.NEXTAUTH_SECRET);
const COOKIE_NAME = 'auth-token';

export async function getAuthUser() {
  const cookieStore = cookies();
  const token = cookieStore.get(COOKIE_NAME)?.value;
  if (!token) return null;
  try {
    const { payload } = await jwtVerify(token, JWT_SECRET);
    return payload as JwtPayload;
  } catch {
    return null;
  }
}
```

### Segurança

- JWT secret mínimo 32 caracteres em produção
- Cookie httpOnly impede acesso via JavaScript
- Expiração de 8h balanceia UX e segurança para MVP
- Nunca logar senha ou hash de senha

### Rotas Protegidas (middleware matcher)

```typescript
export const config = {
  matcher: ['/((?!login|api/auth|_next|favicon).*)'],
};
```

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled

## File List

_A ser preenchido durante implementação_

## Dev Agent Record

### Implementation Notes
_A ser preenchido pelo @dev_

### Completion Checklist
- [ ] Todos os ACs implementados
- [ ] Login funcional com credenciais corretas
- [ ] Redirecionamento funcionando (login → dashboard, sem auth → login)
- [ ] Logout limpa cookie corretamente
- [ ] Senha nunca aparece em logs ou respostas da API
- [ ] Status atualizado para Done
