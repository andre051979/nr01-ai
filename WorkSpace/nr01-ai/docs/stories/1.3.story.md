---
id: "1.3"
title: "Diagnóstico — Cadastro da Empresa e Setores"
epic: 1
story: 3
status: Draft
priority: high
type: fullstack
created: 2026-02-25
updated: 2026-02-25
blockedBy: ["1.2"]
---

# Story 1.3: Diagnóstico — Cadastro da Empresa e Setores

## Status: Draft

## Story Statement

Como administrador, quero cadastrar os dados da empresa (nome, CNPJ, número de funcionários) e seus setores, para que eu possa iniciar o diagnóstico de riscos psicossociais de forma organizada por área.

## Acceptance Criteria

- [ ] **AC1:** Página `/diagnostico` com stepper visual mostrando as 3 abas: "Empresa", "Questionário", "Evidências"
- [ ] **AC2:** Formulário de cadastro da empresa com campos: Nome (obrigatório), CNPJ (obrigatório, máscara XX.XXX.XXX/XXXX-XX, validado), Número de funcionários (obrigatório, inteiro positivo)
- [ ] **AC3:** CNPJ validado com algoritmo oficial (dígitos verificadores) — exibir erro "CNPJ inválido" em tempo real
- [ ] **AC4:** Seção de setores: adicionar setores dinamicamente com nome e número de funcionários opcional
- [ ] **AC5:** Mínimo de 1 setor obrigatório para prosseguir
- [ ] **AC6:** Botão para remover setor individual (mínimo 1 setor sempre mantido)
- [ ] **AC7:** Dados salvos no banco ao clicar em "Salvar e Continuar" — empresa + setores persistidos
- [ ] **AC8:** Se empresa já cadastrada (re-entrada), carregar dados existentes para edição
- [ ] **AC9:** Validação inline em todos os campos obrigatórios

## Tasks / Subtasks

- [ ] **Task 1:** Criar API Routes de empresa (AC: 7, 8)
  - [ ] `GET /api/empresa` — busca empresa do usuário logado (via JWT)
  - [ ] `POST /api/empresa` — cria empresa + setores em transação
  - [ ] `PUT /api/empresa` — atualiza empresa + setores
  - [ ] Validação com Zod em todas as rotas
  - [ ] Usar `prisma.$transaction()` para atomicidade

- [ ] **Task 2:** Criar componente de máscara de CNPJ (AC: 2, 3)
  - [ ] Instalar `react-input-mask` ou implementar manualmente
  - [ ] Função `validarCNPJ(cnpj: string): boolean` com algoritmo oficial
  - [ ] Integrar com react-hook-form via `Controller`

- [ ] **Task 3:** Criar componente de lista dinâmica de setores (AC: 4, 5, 6)
  - [ ] Componente `SetorList` com `useFieldArray` do react-hook-form
  - [ ] Botão "Adicionar Setor" — adiciona novo item à lista
  - [ ] Botão remover por setor (desabilitado quando só 1 setor)
  - [ ] Campos: nome do setor (obrigatório), número de funcionários (opcional)

- [ ] **Task 4:** Criar página `/diagnostico` com stepper (AC: 1)
  - [ ] Componente `DiagnosticoStepper` com 3 etapas
  - [ ] Aba ativa: "Empresa" (step 1)
  - [ ] Steps 2 e 3 desabilitados até empresa salva

- [ ] **Task 5:** Criar formulário de empresa completo (AC: 2, 3, 9)
  - [ ] Criar `packages/web/app/(dashboard)/diagnostico/empresa/page.tsx`
  - [ ] Form com react-hook-form + Zod schema
  - [ ] Feedback visual de sucesso ao salvar
  - [ ] Loading state durante submit

- [ ] **Task 6:** Implementar carregamento de dados existentes (AC: 8)
  - [ ] `useEffect` ao montar o componente: buscar empresa via GET
  - [ ] Pré-preencher form com dados existentes
  - [ ] Indicar visualmente que está em modo edição

## Dev Notes

### Zod Schema da Empresa

```typescript
import { z } from 'zod';

const setorSchema = z.object({
  id: z.string().uuid().optional(),
  nome: z.string().min(2, 'Nome muito curto').max(100),
  numFunc: z.number().int().positive().optional(),
});

const empresaSchema = z.object({
  nome: z.string().min(2).max(255),
  cnpj: z.string().length(14, 'CNPJ deve ter 14 dígitos').refine(validarCNPJ, 'CNPJ inválido'),
  numFunc: z.number().int().positive('Deve ser positivo'),
  setores: z.array(setorSchema).min(1, 'Adicione pelo menos 1 setor'),
});
```

### Algoritmo de Validação de CNPJ

```typescript
export function validarCNPJ(cnpj: string): boolean {
  const nums = cnpj.replace(/\D/g, '');
  if (nums.length !== 14) return false;
  if (/^(\d)\1+$/.test(nums)) return false;

  const calc = (n: string, len: number) => {
    let sum = 0, pos = len - 7;
    for (let i = len; i >= 1; i--) {
      sum += parseInt(n[len - i]) * pos--;
      if (pos < 2) pos = 9;
    }
    const result = sum % 11 < 2 ? 0 : 11 - (sum % 11);
    return result === parseInt(n[len]);
  };

  return calc(nums, 12) && calc(nums, 13);
}
```

### Arquivo de Localização

- Página: `packages/web/app/(dashboard)/diagnostico/page.tsx`
- Componente stepper: `packages/web/components/diagnostico/DiagnosticoStepper.tsx`
- Componente setores: `packages/web/components/diagnostico/SetorList.tsx`
- API: `packages/web/app/api/empresa/route.ts`

### Transação Prisma (criar empresa + setores)

```typescript
const result = await prisma.$transaction(async (tx) => {
  const empresa = await tx.empresa.create({ data: { nome, cnpj, numFunc } });
  const setores = await tx.setor.createMany({
    data: setoresData.map(s => ({ ...s, empresaId: empresa.id }))
  });
  return { empresa, setores };
});
```

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled

## File List

_A ser preenchido durante implementação_

## Dev Agent Record

### Implementation Notes
_A ser preenchido pelo @dev_

### Completion Checklist
- [ ] Todos os ACs implementados
- [ ] CNPJ validado com algoritmos de dígitos verificadores
- [ ] Transação atômica empresa + setores
- [ ] Stepper navegável funcionando
- [ ] Status atualizado para Done
